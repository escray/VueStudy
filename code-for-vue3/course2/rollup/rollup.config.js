const config = {
  input: 'input.js',
  output: {
    file: 'output.js',
    // 指定模块形式
    // IIFE, Immediately Invoked Function Expression
    // 立即调用的函数表达式
    format: 'iife'
  }
}

export default config
